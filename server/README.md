### Node + Express + MongoDB 
### Dockerize App

**Run the server on port 3000**

``` 
npm i
npm run dev
'''

OR

**run in docker** 

```
docker-compose up --build
```

**example env**

> PORT=3000
> MONGODB_URI=